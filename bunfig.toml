[test]
preload = ["./tests/setup.ts"]
coverage = true

# Only run unit tests (exclude e2e and integration)
root = "./tests/unit"
# Coverage settings - only include src/ files
coverageSkipTestFiles = true